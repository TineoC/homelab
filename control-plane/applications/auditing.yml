apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auditing
  namespace: argocd
spec:
  project: control-plane
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  source:
    repoURL: https://github.com/TineoC/homelab.git
    targetRevision: HEAD
    path: control-plane/manifests/auditing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
