websiteName: &websiteName web.tineochristopher.com

image: nginx:alpine

service:
  internalPort: 80
  externalPort: 80
  type: ClusterIP

ingress:
  className: traefik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    external-dns.alpha.kubernetes.io/hostname: tineochristopher.com
    external-dns.alpha.kubernetes.io/target¶: web.tineochristopher.com
    external-dns.alpha.kubernetes.io/ttl: "60"
  tls:
    - hosts:
        - *websiteName
      secretName: staging-tineochristopher-com