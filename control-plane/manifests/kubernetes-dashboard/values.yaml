kubernetes-dashboard:
  protocolHttp: false
  app:
    ingress:
      enabled: true
      issuer:
        name: letsencrypt-prod
        scope: cluster
      tls:
        enabled: true
      annotations:
        nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
        external-dns.alpha.kubernetes.io/target: 96.248.109.98
        external-dns.alpha.kubernetes.io/hostname: dashboard.tineochristopher.com
      hosts:
      - dashboard.tineochristopher.com
      path: /
      pathType: Prefix
      ingressClassName: nginx

oauth2-proxy:
  config:
    provider: keycloak-oidc
    existingSecret: kubernetes-dashboard-client-config
    oidcIssuerURL: https://keycloak.tineochristopher.com/realms/kubernetes
    emailDomains:
    - "*"